{
  "name": "767sqn Fight's On Client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify ./src/index.ts --debug -o ./build/bundle.js -t [ babelify --global true --presets [ @babel/preset-env ] --extensions '.js'] -p [ tsify --noImplicitAny ]",
    "watch": "watchify ./src/index.ts --debug -o ./build/bundle.js -t [ babelify --global true --presets [ @babel/preset-env ] --extensions '.js'] -p [ tsify --noImplicitAny ]"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "tsify": "^5.0.4",
    "typescript": "^5.3.3",
    "watchify": "^4.0.0"
  }
}
